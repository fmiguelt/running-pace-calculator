# running-pace-calculator
Single-file HTML running pace calculator. Converts speed (km/h) to pace (min/km) and back, computes weighted average speed and pace across multi-segment runs, averages per-kilometre splits with 8K/10K presets, and includes a pace/speed reference table. No dependencies — just open it in a browser.
